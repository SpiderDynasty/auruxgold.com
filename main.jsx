import React from "react";
import ReactDOM from "react-dom/client";
import AuctionSite from "./app";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AuctionSite />
  </React.StrictMode>
);
